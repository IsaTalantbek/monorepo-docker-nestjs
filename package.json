{
    "name": "monorepo",
    "private": true,
    "version": "1.0.1",
    "description": "monorepo-docker-nestjs project",
    "license": "ISC",
    "author": "isat",
    "type": "commonjs",
    "main": "docker-compose.prod.yml",
    "scripts": {
        "start:prod": "docker-compose -f docker-compose.prod.yml up --build",
        "start:dev": "docker-compose -f docker-compose.dev.yml up --build",
        "create:app": "node util/create/script.js",
        "docker:clear": "docker system prune -af --volumes",
        "install:app1": "npm install --prefix apps/project1"
    },
    "devDependencies": {
        "prettier": "^3.4.2"
    }
}
